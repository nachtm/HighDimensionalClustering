package spinacht.data;

// hash and equals uq. by elements

/**
 * While this interface contains no actual code, a TransparentSubspace must hash and check equality using the dimensions of
 * the subspace.
 */
public interface TransparentSubspace extends Subspace {

}